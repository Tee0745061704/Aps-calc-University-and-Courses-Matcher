services:
  - type: web
    name: sa-varsity-tracker
    runtime: python
    buildCommand: "pip install -r requirements.txt && python database.py"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
